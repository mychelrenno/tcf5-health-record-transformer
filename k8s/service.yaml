apiVersion: v1
kind: Service
metadata:
  name: tcf5-health-record-transformer
spec:
  selector:
    app: tcf5-health-record-transformer
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP

